#
# Config.yml

config:

# Private message format.

private-messages:
  # Format for private messages.
  from-sender: "&fYou &8» &a%target% &8» &f%message%"
  to-receptor: "&a%sender% &8» &fYou &8» &f%message%"